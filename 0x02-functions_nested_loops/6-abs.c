#include "main.h"
#include <stdlib.h>
/**
 * int _abs - checks if the number is positive, negative or 0.
 * @int: receives number
 * return: 0 for success : returns 1 otherwise
 */
int _abs(int i)
{
    int c = abs(i);
    _putchar(c);
    return (0);
}