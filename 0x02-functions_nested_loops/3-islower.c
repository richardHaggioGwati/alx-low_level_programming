#include "main.h"
/**
 * _islower - checks for lowercase character.
 * c? Returns 1 if c is lowercase : Returns 0 otherwise
 */
int _islower(int c) 
{
    if(c >= 'z' && c <= 'z')
{
    return 1;
} 
else 
{
    return 0
}
}